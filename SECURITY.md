# Security

Report vulnerabilities through the [Mint Shelf security policy](https://github.com/mintshelf/.github/security/policy).

Keep production credentials in Cloudflare Worker secrets and local credentials in `.dev.vars`. Do not commit either.
